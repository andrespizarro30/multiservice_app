import 'package:flutter/material.dart';

class AcceptedServices extends StatelessWidget {
  const AcceptedServices({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
      child: Center(
        child: Text("Accepted Services"),
      ),
    );
  }
}

